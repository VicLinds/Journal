import React from 'react';

const Navbar = () => {
  return <div className="flex">
      <button>Home</button>
      <button>Contact</button>
      <button>Index</button>
  </div>;
};

export default Navbar;
